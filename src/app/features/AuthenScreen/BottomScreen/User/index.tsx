import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const UserTabScreen = (props: Props) => {
  return (
    <View>
      <Text>UserTabScreen</Text>
    </View>
  )
}

export default UserTabScreen

const styles = StyleSheet.create({})